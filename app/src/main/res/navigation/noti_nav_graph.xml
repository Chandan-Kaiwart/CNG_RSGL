<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/noti_nav_graph"
    app:startDestination="@id/notiListFrag">

    <fragment
        android:id="@+id/notiListFrag"
        android:name="com.apc.cng_hpcl.home.newNotif.NotiListFrag"
        android:label="NotiListFrag" >
        <action
            android:id="@+id/action_notiListFrag_to_notiDetFrag"
            app:destination="@id/notiDetFrag" />
        <argument
            android:name="mgr_id"
            app:argType="string" />
    </fragment>
    <fragment
        android:id="@+id/notiDetFrag"
        android:name="com.apc.cng_hpcl.home.newNotif.NotiDetFrag"
        android:label="NotiDetFrag" >
        <argument
            android:name="data"
            app:argType="string" />
    </fragment>
</navigation>